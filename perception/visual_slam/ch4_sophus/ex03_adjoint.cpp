// ch4 练习 3(习题代码部分):数值验证 SO(3) 伴随性质,体会左/右扰动
//
// 伴随性质(SO(3) 的伴随 Ad_R = R):
//         R * exp(p^) * R^T = exp( (R p)^ )
//   含义:把一个"切空间向量 p"用 R 旋转,等价于先把 p 本身用 R 转过去再取指数。
//   这是【左扰动 <-> 右扰动 互相转换】以及 SE(3) 伴随矩阵的基础,务必动手验一遍。
//
// 依赖:Sophus、Eigen3。

#include <iostream>
#include <Eigen/Core>
#include <Eigen/Geometry>
#include "sophus/so3.hpp"

using namespace std;
using namespace Eigen;

int main() {
    // 任取一个旋转 R 和一个(切空间)向量 p
    Matrix3d R = AngleAxisd(0.7, Vector3d(0, 0, 1)).toRotationMatrix();
    Vector3d p(0.1, 0.2, 0.3);

    // ============================================================
    // 1. 左边:R * exp(p^) * R^T
    //    提示:Sophus::SO3d::exp(p).matrix() 即 exp(p^) 的 3x3 矩阵
    // ------------------------------------------------------------
    // TODO 1: Matrix3d lhs = R * Sophus::SO3d::exp(p).matrix() * R.transpose();

    // ============================================================
    // 2. 右边:exp( (R p)^ )
    // ------------------------------------------------------------
    // TODO 2: Matrix3d rhs = Sophus::SO3d::exp(R * p).matrix();

    // ============================================================
    // 3. 比较:(lhs - rhs).norm() 应 ≈ 0,验证 Ad_R = R
    // ------------------------------------------------------------
    // TODO 3: 打印误差范数

    // ============================================================
    // 4.(选做)同一旋转,左扰动 vs 右扰动 对点 p 的作用
    //    左扰动:exp(dphi^) * R * p     右扰动:R * exp(dphi^) * p
    //    对照解析雅可比:
    //      左扰动  d(Rp)/d(dphi) = -(R p)^
    //      右扰动  d(Rp)/d(dphi) = -R * p^
    //    用很小的 dphi 数值求差商,和上面解析式对比。
    // ------------------------------------------------------------
    // TODO 4: 取 dphi = 1e-6 * e_k,数值验证左右扰动雅可比

    cout << "[ex03] 骨架已就绪,按 TODO 验证伴随性质与左右扰动。\n";
    return 0;
}
